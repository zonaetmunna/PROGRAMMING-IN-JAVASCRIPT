/* removing element form begging of the array with shift methods */

let numbers = [1, 2, 3, 4];
let removedNumber = numbers.shift();
console.log(removedNumber);  // Output: 1
console.log(numbers);        // Output: [2, 3, 4]
